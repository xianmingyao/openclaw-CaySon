# 2026-03-27 日志

## 今日主要工作

### 1. 多智能体开发逻辑分享
给宁兄梳理了多智能体的核心架构：Orchestrator、角色定义、通信机制（串行/并行/层级）、共享记忆层。

### 2. CLI-Anything 架构深入学习

深入研究了 [HKUDS/CLI-Anything](https://github.com/HKUDS/CLI-Anything) 的架构：

**核心结构**：
- `agent-harness/` - CLI 包根目录
  - `setup.py` - pip install 入口，定义 `console_scripts` 入口点
  - `cli_anything/<software>/` - 主包
    - `__init__.py` - 包初始化 + 版本
    - `__main__.py` - `python -m` 入口
    - `<software>_cli.py` - Click CLI 主程序
    - `README.md` - 使用文档
    - `utils/repl_skin.py` - REPL 样式（ANSI 颜色 + prompt_toolkit）
    - `core/` - 核心模块（API/subprocess 封装）
    - `skills/SKILL.md` - AI Agent 发现文件
    - `tests/` - pytest 测试

**关键设计模式**：
1. **Click 装饰器分组**: `@cli.group()` + `@cli.command()`
2. **全局状态**: `_json_output`, `_repl_mode`, `_last_result`
3. **Error Wrapper**: `@handle_error` 装饰器统一错误处理
4. **REPL Skin**: ANSI 颜色 + prompt_toolkit + history
5. **subprocess 隔离**: 防止 stderr 污染父进程

**REPL Skin 核心组件**：
- `_ACCENT_COLORS` - 每个软件独特配色
- `ReplSkin.prompt()` - 构建提示符
- `ReplSkin.get_input()` - prompt_toolkit 或 input() 回退
- `_ANSI_256_TO_HEX` - 颜色映射用于 prompt_toolkit Style

### 3. windows-control 技能重构

基于 CLI-Anything 架构重构 `windows-control` 技能：

**解决的问题**：
1. **jingmai-agent Pydantic ActionResult** → 内部 `json.loads(result.data.json())` 转换
2. **PowerShell 把 stderr 当错误** → subprocess 隔离 + `sys.stderr = open(os.devnull, 'w')`
3. **ctypes callback RECT bug** → 定义正确的 `_fields_ = [...]`
4. **loguru ANSI 日志噪声** → `os.environ["LOGURU_LEVEL"] = "ERROR"` + subprocess 隔离
5. **jingmai-agent `crop_to_active_window` 不存在** → 直接用 `pyautogui.screenshot()` 绕过

**最终架构**：
```
agent-harness/
├── setup.py
├── cli_anything/windows_control/
│   ├── windows_control_cli.py   # Click CLI (28个命令, 8个分组)
│   ├── repl_skin.py           # REPL 皮肤
│   ├── skills/SKILL.md         # AI 发现
│   ├── tests/test_cli.py       # 测试
│   └── README.md
```

**测试通过的命令**：
- `system info` ✅ - 系统信息
- `mouse click` ✅ - 鼠标点击
- `keyboard press` ✅ - 键盘按键
- `window list` ✅ - 窗口列表 (22个窗口)
- `ui screenshot` ✅ - 截图 1920x1080
- `file exists` ✅ - 文件存在检查
- `--json` 模式 ✅ - JSON 输出

### 4. Claude Code 生态调研
- 分析了 @程序员Sunday 的 Claude Code 生态视频
- 收集了 4 个工具：claude-hud、get-shit-done、learn-claude-code、claude-code-action

### 5. 技术教训

**PowerShell stderr 问题**：
- PowerShell 把所有 stderr 输出当作 "native command error"，即使只是日志
- 解决方案：用 `subprocess.run()` 包装 action 执行，在子进程中 `sys.stderr = open(os.devnull, 'w')`
- 另一个方案：设置 `os.environ["LOGURU_LEVEL"] = "ERROR"` 抑制日志

**ctypes WINFUNCTYPE callback**：
- 回调函数中不能定义 ctypes Structure 类（需要先定义再实例化）
- 必须用 `try/except` 包裹回调内的所有代码，否则异常会导致 EnumWindows 崩溃
- callback 闭包中引用的变量需要是线程安全的

**jingmai-agent bug**：
- `ui_collect_actions.py` 中 `_enum_windows_proc` 的 `results` 是 `LP_c_long` 类型（ctypes 指针），不是 Python list
- `CaptureWindowScreenshotAction` 导入了不存在的 `crop_to_active_window`
- 都需要在 CLI 层做 workaround

### 3. OpenClaw Skill 调研
- 分析了 @AI干货局 的底座级技能推荐视频
- 收集了 Skill 01-06 的情报（agent-browser、self-improving、skill-vetter、email-management 等）

### 4. GitHub 爆款项目情报（@AI未来·今日增量榜）
| 排名 | 项目 | ⭐ | 方向 |
|------|------|-----|------|
| #1 | deer-flow | 46.2k | 字节SuperAgent |
| #2 | litellm | 40.6k | LLM网关 |
| #4 | ruview | 42.3k | WiFi感知 |
| #6 | TradingAgents-CN | 21.3k | 金融多Agent |

### 5. GitHub 周榜情报（@赛博笔记）
| 排名 | 项目 | ⭐ | 方向 |
|------|------|-----|------|
| #1 | everything-claude-code | 18.8k | Claude配置 |
| #3 | x-algorithm | 12.3k | X推荐算法 |
| #11 | Eigent | - | 多Agent协作 |
| #17 | get-shit-done | 39.3k | 元提示词 |

### 6. 安装了三个 OpenClaw Skill
- ✅ `openclaw-agent-browser` (1.0.0)
- ✅ `openclaw-skill-vetter-1-0-0` (1.0.0)
- ✅ `claw-self-improving` (1.2.10)

### 7. Skill 编写方法论（@溪树知识）
好的 Skill = 定场景 + 立目标 + 理规则 + 给示例 + 划边界

## 技能安装记录

| 技能 | 安装日期 | 状态 | 备注 |
|------|---------|------|------|
| agent-browser | 2026-03-27 | ✅ 已装 | 浏览器自动化 |
| skill-vetter | 2026-03-27 | ✅ 已装 | 安全审查 |
| self-improving | 2026-03-27 | ✅ 已装 | 自我进化 |

## 遇到的问题
- 安装 `self-improving-1-2-16` 失败（版本名不对），改用 `claw-self-improving` 成功

## 定时任务设置（已更新为22:30）
- ✅ 创建 OpenClaw Cron Job：`daily-git-commit`
  - 执行时间：每天 22:30（Asia/Shanghai）
  - 操作：`cd E:\workspace && git add . && git commit -m '定时自动提交' && git push`
  - Job ID：d17e9c1c-7e7f-4b5d-8f3a-c6b2e1d0a987
  - 下次执行：今天 22:30
- ✅ 创建 OpenClaw Cron Job：`daily-youdao-summary`
  - 执行时间：每天 22:30（Asia/Shanghai）
  - 操作：读取今日学习日志 → 使用 agent-browser 操作有道云笔记 → 写入「技术知识库」文件夹
  - Job ID：5b756acc-89d9-4dd7-9b00-df35e6010f86
  - 下次执行：今天 22:30
  - 注意：如果有道云笔记未登录，先截图二维码发给用户

## windows-control 技能改进学习记录（20:30-21:00）

### 学习来源
研究 `E:\PY\UFO` 源码，发现其使用 `pywinauto` + 正确的窗口激活机制

### 关键改进点
1. **使用 pywinauto** 进行窗口管理（比 pyautogui 更强大）
2. **使用 Win32 API** `SetForegroundWindow` 正确激活窗口
3. **先激活窗口再点击** 的流程

### 发现的问题
有道云笔记网页版对 pyautogui/pywinauto 点击不响应
- 原因：网页使用 JavaScript 处理点击事件
- 可能的解决方案：
  1. 使用 Selenium/Playwright 控制浏览器
  2. 直接调用有道云笔记 API
  3. 用户手动点击一次后，AI 继续操作

### 创建了改进版脚本
- `E:\workspace\skills\windows-control\windows_control_v2.py` - 基于 UFO 架构的改进版
- 使用 pywinauto + Win32 API

### pywinauto 安装成功
- `pip install pywinauto` 已安装

## 下午补充记录（18:00-18:30）

### 定时任务调整
- ✅ 删除旧的 23:30 cron 任务
- ✅ 重建两个 22:30 定时任务：
  1. `daily-git-commit`（Job ID: d17e9c1c-7e7f-4b5d-8f3a-c6b2e1d0a987）
  2. `daily-youdao-summary`（Job ID: 5b756acc-89d9-4dd7-9b00-df35e6010f86）

### 有道云笔记格式规范
- ✅ 分析了 https://share.note.youdao.com/s/GKx09cna 模板
- ✅ 使用 agent-browser 成功访问有道云笔记
- ✅ 格式规范已写入 AGENTS.md：
  - 标题：H1/H2 Markdown
  - 链接展示完整URL
  - 列表用 `-` 序号
  - 重点术语加粗或反引号
  - 结构：GitHub项目 / Skills / 技术总结 / 下一步

### agent-browser 技能首次使用
- ✅ 成功用 `npx agent-browser open <url>` 打开网页
- ✅ 成功用 `npx agent-browser snapshot -i` 获取交互元素
- ✅ 成功用 `npx agent-browser screenshot` 截图
- 遇到问题：直接执行 `agent-browser` 命令不行，需用 `npx agent-browser`

### 宁兄新指示
"每天22:30统计学习的技术栈，好用的github、好用的skills、最新的技术架构、技术实现文档，自我学习后总结使用浏览器操作记录到有道笔记技术知识库文件夹。有道笔记未登录需截图反给登录二维码给宁兄扫码。"

#### 定时任务补充
- ✅ 创建 `morning-wechat-login-check`（Job ID: 584353fb-d6d7-40f2-8d14-4de85d0461e6）
  - 执行时间：每天早上 9:00（Asia/Shanghai）
  - 操作：检查微信登录状态 → 未登录则运行 `openclaw channels login --channel openclaw-weixin` → 发送登录链接给用户
  - 下次执行：明天早上 9:00

## 待办事项
- [ ] 今晚22:30验证cron任务执行效果
- [ ] 验证有道云笔记写入功能
- [ ] 测试agent-browser在有道云笔记创建笔记的完整流程
- [ ] 明天9:00验证微信登录检查任务

## 知识库建设
- ✅ 创建 `knowledge/` 目录
- ✅ 创建 `knowledge/github-projects.md` - 27个GitHub项目情报库
- ✅ 创建 `knowledge/README.md` - 知识库索引

## 下一步行动
1. 在实际任务中使用新装的三个 Skill
2. Clone 研究 `deer-flow` 或 `Eigent` 的源码
3. Clone 研究 `litellm` 的 LLM 网关实现
4. 整理一份完整的项目推荐清单

## 宁兄的重要指示
"你在学习，主要这些推给你学习，有好用的 Skills 就安装，然后你自己使用安装的技能，更好的掌握。你在学习使用过程中遇到什么问题，需要主动给我说。你不要存在记忆中，你要保存在行为习惯的准则中，不然你会忘记的。"

→ 已将此指示写入 AGENTS.md 的 "Skill 学习与成长准则" 章节
