# 2026-06-23 工作日志

## 09:00 morning-wechat-login-check 执行情况

### 任务结果
**状态异常，无法执行标准 login 流程**（并非 login 失败，是底层连接问题）

### 关键发现

1. **Status 显示正常**（假阳性）
   - `openclaw channels status` 显示 `openclaw-weixin 6a4a8adda83b-im-bot: enabled, configured, running, in:22m ago`
   - 三个绿灯全亮，但实际功能异常

2. **底层 getUpdates 持续超时**
   - 错误：`weixin getUpdates error (1/3): Error: getUpdates fetch failed url=https://ilinkai.weixin.qq.com/ilink/bot/getupdates timeoutMs=35000`
   - 错误代码：`ETIMEDOUT 198.18.0.6:443`
   - 频率：昨晚到今早连续 20+ 次失败
   - 错误文件：`subsystem-C-H8Q21Y.js:178` / `monitor.js:46`

3. **根因诊断**
   - DNS 解析：`ilinkai.weixin.qq.com` → `198.18.0.6`（FlClash 代理地址）
   - 网络接口：`InterfaceAlias: FlClash`
   - TCP 测试：`TcpTestSucceeded: True`（端口通但应用层失败）
   - 结论：**FlClash 代理规则导致微信 bot API 无法正常通信**

4. **无法自动修复**
   - `openclaw channels login` 是交互式命令，exec 工具拒绝执行
   - 当前无 qrcode/bot_type 登录链接产生
   - bot 状态字段全绿但功能不工作

### Cron 任务设计缺陷
- 任务假设 login 会自动产生 qrcode 链接，但实际：
  - login 命令需 TTY 交互（扫码）
  - 即使尝试 login，前置的 getUpdates 超时也会导致流程卡住
- 任务应在检查时区分：
  - "未登录"（需要登录） vs
  - "网络异常"（需要排查代理/防火墙）

### 后续建议（待宁兄确认）
- 选项 A：排查 FlClash 规则，把 `ilinkai.weixin.qq.com` 加入直连名单
- 选项 B：用户手动在终端跑 `openclaw channels login --channel openclaw-weixin` 扫码
- 选项 C：检查微信 bot token 是否过期（`openclaw channels logs` 看更多上下文）

### 已通知宁兄
详细报告 + 三个选项建议，等待决策。
