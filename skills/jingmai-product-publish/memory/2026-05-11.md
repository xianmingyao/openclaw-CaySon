## Reliability Task Board

- Context: Improve real usability of `湖南上架表格.xlsx -> 京麦上架` flow.
- Goal: Raise completion score by fixing runtime judgment, recovery, and batch execution semantics.

### Active Workstreams

- `navigate_to` precheck false negatives
  - Problem: Page is already inside publish flow, but precheck still blocks execution.
  - Status: in_progress

- `doc_strict` page-state tolerance
  - Problem: Current step fails when the UI has already advanced to the next valid page.
  - Status: in_progress

- `OpenCLI` recovery probe integration
  - Problem: Probe runs, but its result is not used to unblock recovery.
  - Status: in_progress

- Batch vs single execution semantics
  - Problem: `publish_mode=batch` exists, but execution evidence is still weak.
  - Status: in_progress

- Acceptance evidence quality
  - Problem: Need clearer runtime evidence for why a step passed or failed.
  - Status: pending

### This Session

- Read graph report, planner/executor/scraper/test coverage, and recent acceptance artifacts.
- Confirmed latest acceptance still fails on `湖南上架表格.xlsx`.
- Started patching executor coercion, doc-strict tolerance, and OpenCLI probe handoff.
