# 2026-06-09

## jm_ufo_agent v2 gstack 剩余缺口继续推进

- 继续修复 F01/F04/F05/F07/F13/F14/F17/F19 和真实 E2E 启动门控。本轮仍不执行真实京麦点击、京东访问、VLM/MiniMax 调用或保存草稿。
- 新增 `ExcelProductImportService` 和 CLI `import-excel`，可解析真实 xlsx 并输出 parsed/written/generated-id 行摘要；写入层通过 `ProductWriter` 协议接 `ProductRepository`，测试用 fake repo。
- 新增 `Win32WindowInspectorBackend`，通过可注入 API 读取窗口标题、类名和子窗口摘要；CLI `inspect-jingmai-window` 只读枚举真实窗口事实，不点击不输入。
- 新增 `runtime/evidence.py`，统一 halt 和草稿验证证据结构；`GraphState.halt()` 保留旧 details 兼容，同时提升 screenshot/page_signature/window_summary 等标准字段。
- `interactive.py` 支持多帧 `render_live_dashboard()`，CLI `live-dashboard --states-jsonl` 可回放状态流，向 Rich 实时 dashboard 前进了一步。
- 新增 `runtime/review.py`，用标准库 urllib transport + `RetryingReviewTransport` 构造 MiniMax-M3 生产评审策略；真实网络调用仍需 workflow 显式注入和配置 token。
- `production.py` 新增 `assess_production_readiness()`，真实 E2E 启动前必须检查 9 个阶段能力，不再把 adapter 骨架误当生产闭环。
- 验证：`python -m pytest` 为 79 passed；`uv run --extra test python -m pytest` 为 77 passed, 2 skipped；安全扫描、compileall、graphify update 均通过，graphify 为 805 nodes / 1980 edges。

## jm_ufo_agent v2 gstack 真实缺口边界修复

- 继续按 `gstack` 流程修复 F02/F04/F05/F13/F14、WebView、Redis/Milvus 和生产运行闭环缺口；本轮仍坚持真实账号/真实桌面/真实外部服务默认不执行，只落地可注入 adapter 和安全门控。
- 新增/完善：`JdPageParser`、`JdCrawlerAgent` 注入 HTML/transport 的京东解析边界；`ImageFetchAgent` 显式 downloader 才联网写文件；`ufo_adapter.py` 暴露 UFO v1 来源路径和京麦窗口事实模型。
- `JmHostAgent` 支持 Qt51511QWindowIcon、Qt 子窗口、WebView Pane 摘要、登录态提示、新增商品页标题验证；`JmWebViewAgent` 通过 `WebViewFormLoop` 串起截图 OCR、坐标、剪贴板、读回和相似度证据。
- 新增 `RedisBusinessCache` 和 `MilvusReflectionStore`，把 OCR cache、VLM cache、locator cache、failure reflections 的业务 key/写入形状固定下来，但不隐藏真实模型向量生成或外部连接。
- CLI 增加 `dashboard`、`inspect-ufo`、`run --backend ufo-observe` 安全门控；真实 backend 必须 `--confirm-real-jingmai` 且当前只允许 observe-only。
- 新增 `tests/test_v2_real_gap_boundaries.py`，验证结果：`python -m pytest` 为 73 passed；`uv run --extra test python -m pytest` 为 72 passed, 1 skipped；安全扫描通过；`graphify update .` 更新到 714 nodes / 1739 edges。
- 重要边界：这仍不是“真实生产自动上架闭环”。尚未实际执行真实 Excel -> 京东抓取 -> 图片/VLM -> 京麦填写 -> 保存草稿 -> 读回验证。

## jm_ufo_agent v2 gstack 缺口开发

- 使用 `gstack` 技能分析 `docs/superpowers/specs/2026-06-09-jm-ufo-agent-v2-design.md` 后，选择先开发本地可测的状态机、持久化证据和 VLM halt 边界，不触碰真实京麦/京东/MiniMax 账号。
- 新增 18 个 workflow 节点文件，并在 `workflow/graph.py` 增加 `build_state_graph()`；有 `langgraph` 时注册真实 StateGraph，缺依赖时返回 `LocalStateGraphSpec` 供测试验证节点完整性。
- `DryRunWorkflow` 新增 `PREPARE_ASSETS` 步骤，默认 dry-run；注入失败 handler 时，`ImageTransformAgent.transform_with_retry()` 会在 VLM 连续失败 3 次后返回失败结果并 halt 当前 row。
- `GraphState.halt()` 扩展为带证据中止，新增 `halt_evidence` 列表，记录 node、reason、details，后续真实截图/OCR/page signature 可统一写入 details。
- 补齐 5 个业务 repository：`ProductAssetRepository`、`StepLogRepository`、`LocatorCacheRepository`、`DraftVerificationRepository`、`VlmCallRepository`；同时补齐对应 record 数据模型。
- `jm_product_assets` 新增 `uk_jm_product_assets_identity` 唯一键，避免 upsert 语义没有数据库约束支撑。
- 新增 `tests/test_v2_state_graph_and_halt.py`，并扩展 repository 测试；验证结果：`python -m pytest` 为 `64 passed`，`uv run --extra test python -m pytest` 为 `63 passed, 1 skipped`，安全扫描通过，`graphify update .` 更新到 600 nodes / 1386 edges。

## jm_ufo_agent v2 Phase 1 基础设施

- 根据 `docs/superpowers/specs/2026-06-09-jm-ufo-agent-v2-design.md`，本轮先落地 Phase 1：配置、存储客户端、Repository、SafetyPolicy、价格/向量/重试工具。
- 新增 `jm_ufo_agent/` 包，配置加载不依赖第三方 dotenv，也不会在导入时连接 MySQL、Redis、Milvus，方便 dry-run 和单元测试。
- `SafetyPolicy` 默认硬阻断发布、删除、下架等危险动作；价格写入必须携带采购价和市场价证据，并限制在 `采购价 * 0.9` 到 `市场价 * 1.1`。
- Repository 采用传入连接对象的方式，单元测试通过 fake connection 覆盖 SQL 生成和 JSON 状态恢复，不依赖真实数据库。
- 验证结果：`python -m py_compile ...` 通过；`python -m pytest` 通过，16 个测试全部成功；`graphify update .` 已更新图谱。
- 风险与后续：真实 GUI/京麦窗口、UFO v1 底层动作适配、LangGraph 节点和 MiniMax-M3 评审循环尚未实现，需要按 Phase 2-4 继续拆分。

## jm_ufo_agent v2 Phase 2-5 dry-run 闭环

- 继续实现 Agent/Strategy/Workflow/Runtime/CLI 干跑闭环：`Agent` 层只暴露确定性方法，`DesktopAgent` 的 `click/fill/submit` 统一先走 `SafetyPolicy`，默认 backend 只记录命令，不触碰真实京麦窗口。
- 新增 `ObserveStrategy`、`DeterministicPlanStrategy`、`GuardedActionStrategy`、`VerifyStrategy`、`FormCompletionScoreStrategy`、`MiniMaxReviewScoreStrategy`、`FailureReflectionStrategy`，先以本地确定性逻辑覆盖证据链和保存草稿闸门。
- 新增 `DryRunWorkflow`，按 `BOOTSTRAP -> OBSERVE_PAGE -> PLAN_FIELDS -> FILL_AND_VERIFY_FIELDS -> ASSESS_FORM_COMPLETION -> MINIMAX_REVIEW_SCORE -> SAVE_DRAFT -> VERIFY_DRAFT` 顺序运行；完成度不足或 blocker 存在时 halt，完成度达标时只保存草稿。
- 新增 `jm-ufo-agent dry-run` CLI，并支持 `--product-json @path.json`，兼容 Windows PowerShell 写出的 UTF-8 BOM JSON 文件。
- 验证结果：`python -m py_compile ...` 通过；`python -m pytest` 通过，28 个测试全部成功；CLI 文件参数 dry-run 输出 `status=saved_draft`；`graphify update .` 已更新图谱。
- 后续风险：真实 UFO v1 backend 复制/适配、真实截图/OCR、MySQL checkpoint 写入、Redis 行锁、MiniMax-M3 OpenAI-compatible API 调用仍未接外部系统，需要单独小步实现和人工确认。

## jm_ufo_agent v2 后端边界与运行时 worker

- 继续补齐具体 Command 构造器：`FillCommand`、`ClickCommand`、`NavigateCommand`、`UploadCommand`、`ReadCommand`，它们只生成数据对象，不直接执行 UI。
- 新增 `backends/web_surface/*`：坐标计划、截图 OCR 结果模型、剪贴板填充 dry-run 服务、局部读回验证；默认不触碰真实屏幕或系统剪贴板。
- 新增 `AsyncMySQLSaver`，通过 `TaskRepository` 保存/读取 `GraphState` 快照，为后续 LangGraph/MySQL checkpoint 接入留出边界。
- 新增 `RedisRowLock`，封装 Redis `SET NX EX` 行级锁，并在 release 时校验 owner，避免误删其他 worker 的锁。
- 新增运行时 worker：`DataFetchWorker` 用 semaphore 控制抓取并发，`ImageProcessWorker` 用 maxsize=1 队列串行图片/VLM 任务，`GuiWorker` 用 `GuiLock` 串行 GUI 操作。
- 命名修正：非桌面执行入口避免使用 `fill/submit` 方法名，安全扫描现在只命中 `DesktopAgent.click/fill/submit` 三个统一受 `SafetyPolicy` 保护的方法。
- 验证结果：`python -m py_compile ...` 通过；`python -m pytest` 通过，37 个测试全部成功；CLI dry-run 仍输出 `status=saved_draft`；`graphify update .` 已更新图谱。

## jm_ufo_agent v2 评审/Excel/checkpoint 边界

- 新增 `MiniMaxReviewClient`，支持 `/models` preflight、OpenAI-compatible chat/completions payload 构造、模型 JSON 输出解析；测试使用 fake transport，不访问真实 MiniMax。
- 新增 `ExcelProductParser`，显式调用时才读取 xlsx；优先使用 `openpyxl`，缺依赖时抛明确错误；可把 Excel 行转换为 `ProductRecord`。
- `FieldProgressRepository` 新增 `list_verified_fields(task_id, row_index)`，只返回 `status='verified'` 字段，用于字段级断点续传。
- `SQLRepository` 新增 `fetchall`，兼容 fake connection 和真实 cursor。
- `DryRunWorkflow` 支持注入 `AsyncMySQLSaver`，在关键节点后保存 checkpoint；默认不注入时仍保持纯内存 dry-run。
- 修复一次循环依赖：`storage.__init__` 和 `workflow.__init__` 不再 eagerly import 子模块，`workflow.graph` 只在 TYPE_CHECKING 下引用 `AsyncMySQLSaver`。
- 验证结果：`python -m py_compile ...` 通过；`python -m pytest` 通过，45 个测试全部成功；CLI dry-run 输出 `status=saved_draft`；安全扫描只命中受保护的 `DesktopAgent.click/fill/submit`；`graphify update .` 已更新图谱。

## jm_ufo_agent v2 本轮剩余缺陷修补

- 使用 `planning-with-files` 代替当前不可用的 `writing-plans` 技能，建立 `task_plan.md`、`findings.md`、`progress.md` 追踪任务。
- 核心字段从 6 个扩到 12 个：`title/category/brand/sku/jd_price/purchase_price/market_price/main_image/sub_images/description/weight/stock`。
- 新增 `RowExecutionRepository` 和 `ArtifactRepository`，MySQL DDL 扩展为 12 张表。
- `DryRunWorkflow` 接入 SELECT_ROW、RECOVER、字段进度写入、row82 seed 规则和 committed 行状态写入。
- 新增 `python -m jm_ufo_agent run` 入口，默认仍为 dry-run，不触碰真实京麦。
- 新增 `scripts/check_safety_policy.py`，用 AST 检查 `click/fill/submit` 是否在方法开头调用 `assert_allowed`。
- MiniMax 评审策略支持注入 `MiniMaxReviewClient`，测试使用 fake transport，未访问真实 MiniMax。
- 验证结果：`python -m pytest` 为 `55 passed`；`uv run --extra test python -m pytest` 为 `54 passed, 1 skipped`；CLI run、安全扫描和 `graphify update .` 均通过。

## jm_ufo_agent v2 缺陷修复规划

- 使用 `planning-with-files` 技能规划 12 个主要缺陷的修复路线。
- 新增计划文档：`docs/superpowers/plans/2026-06-09-jm-ufo-agent-v2-gap-closure-plan.md`。
- 下一轮分 10 个阶段：LangGraph StateGraph、Repository 补全、UFO/UIA/Win32 backend、WebView fallback、真实保存草稿、京东抓取与图片处理、MiniMax 生产闭环、Rich dashboard、halt 证据标准化、生产验证。
- 关键决策：先补状态机和持久化证据，再接真实桌面 backend；所有外部系统测试默认 fake，真实调用必须显式环境变量和人工确认。
